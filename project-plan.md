# Handsign 프로젝트 기획서

## 1. 프로젝트 정의

### 한 줄 정의
브라우저 카메라로 손 랜드마크와 제스처를 실시간 인식하고, 이를 시각과 음향으로 변환하는 인터랙티브 미디어아트 웹앱을 만든다.

### 작업 가설
- 사용자는 별도 설치 없이 웹에서 즉시 체험할 수 있어야 한다.
- 첫 버전은 "작품성"보다 "인터랙션이 안정적으로 반응한다"는 신뢰를 먼저 증명해야 한다.
- 카메라 프레임은 서버로 전송하지 않고 브라우저 내부에서만 처리한다.
- 1차 타깃은 최신 Chrome 계열 데스크톱이며, iOS Safari는 2차 안정화 대상으로 둔다.

### 프로젝트 목표
- 손 동작 3종 이상이 시각과 음향 변화로 명확하게 연결되는 경험을 구현한다.
- 첫 진입 후 10초 이내에 체험을 시작할 수 있는 온보딩을 만든다.
- 메인 경험 구간에서 렌더링은 시각적으로 끊기지 않고, 입력 반응은 체감상 즉각적이어야 한다.
- 이후 커스텀 제스처와 추가 씬 확장이 가능한 구조를 초기에 잡는다.

## 2. 경험 콘셉트

### 워킹 타이틀
`Handsign: 손짓으로 빛과 소리를 연주하는 브라우저 퍼포먼스`

### 핵심 경험
- 사용자가 손을 카메라 앞에 올리면 화면 속 입자 장(field)이 반응한다.
- 손가락을 오므리는 동작은 입자 밀도와 사운드 필터를 조절한다.
- 특정 제스처는 씬 전환 또는 효과 전환 트리거로 사용한다.
- 작품은 "조작 UI"보다 "몸짓에 반응하는 악기"처럼 느껴져야 한다.

### MVP에서 보여줄 감각
- 시각: 손 주변을 따라다니는 파티클, 잔상, 빛 번짐
- 음향: 드론 또는 패드 계열의 지속음, pinch 값에 따른 cutoff/pitch 변화
- 상호작용: `Open Palm`, `Pinch`, `Victory` 3종을 우선 지원

## 3. 사용자 시나리오

### 1차 사용자
- 전시 관람객
- 포트폴리오/데모를 보는 클라이언트 또는 협업자
- 인터랙티브 웹 작업을 검토하는 크리에이티브 개발자

### 대표 흐름
1. 사용자가 랜딩 화면에서 작품 소개와 권한 안내를 본다.
2. `Start Experience`를 누르면 카메라와 오디오 사용 이유를 먼저 설명한다.
3. 권한 허용 후 카메라가 켜지고 손 인식이 시작된다.
4. 화면 속 파티클과 사운드가 손의 위치와 제스처에 따라 반응한다.
5. 사용자는 우측 상단 또는 하단의 작고 명확한 가이드로 사용 가능한 제스처를 확인한다.
6. 세션 종료 시 카메라와 오디오를 해제하고 다시 시작할 수 있다.

## 4. MVP 범위

### 포함
- 카메라 권한 안내와 실패/거부 복구 UX
- 단일 손 기준의 안정적인 랜드마크 추적
- 기본 제스처 3종 처리
- 하나의 대표 비주얼 씬
- 하나의 대표 오디오 시스템
- 시작, 실행, 정지, 재시작이 가능한 세션 수명주기
- 디버그 모드에서 FPS/제스처 상태/랜드마크 오버레이 확인

### 제외
- 멀티 유저 또는 WebRTC 스트리밍
- PWA 설치 기능
- WebXR 연동
- 커스텀 학습 모델
- 녹화/저장/공유 기능
- 관리자 화면, 계정, 서버 API

## 5. 성공 기준

### 제품 기준
- 처음 보는 사용자가 설명 없이도 30초 안에 체험을 시작할 수 있다.
- 최소 3개의 손 동작이 서로 다른 결과를 만든다는 점을 바로 인지할 수 있다.
- 권한 거부 또는 카메라 미지원 상황에서도 사용자가 이유와 다음 행동을 이해할 수 있다.

### 기술 기준
- Chrome 데스크톱 기준 렌더링 55~60fps, 추론 24fps 이상을 목표로 한다.
- iPhone Safari 기준 렌더링 30fps 내외, 추론 15fps 이상을 하한선으로 둔다.
- 오디오 시작은 사용자 액션 직후 1회로 제한하고 자동재생 오류가 없어야 한다.
- 카메라 프레임 및 손 데이터는 네트워크로 전송하지 않는다.

## 6. 기술 방향

### 선택 스택
- 앱 셸: `Vite + TypeScript + React`
- 손 인식: `MediaPipe Tasks Vision Gesture Recognizer`
- 제스처 보강: 랜드마크 기반 커스텀 계산(`pinch distance`, `hand velocity`)
- 비주얼: `three.js`
- 디버그 오버레이: `Canvas2D`
- 오디오: `Tone.js`
- 스레딩: `Web Worker` 기반 추론 분리

### 선택 이유
- `Gesture Recognizer`는 랜드마크와 canned gesture를 함께 다룰 수 있어 MVP 구현 속도가 빠르다.
- `three.js`는 파티클, 후처리, 향후 XR 확장까지 고려했을 때 범용성이 높다.
- `Tone.js`는 브라우저 오디오 정책 대응과 합성/스케줄링에 유리하다.
- 추론을 워커로 분리해야 메인 렌더링과 UI 응답성을 지킬 수 있다.

### 폴백 전략
- Gesture 분류 안정성이 떨어지면 `Hand Landmarker + 커스텀 규칙`으로 단순화한다.
- Safari에서 워커/캔버스 제약이 크면 메인 스레드 디버그 모드로 일단 동작을 확보한다.
- 고급 성능 최적화는 MVP 이후 `OffscreenCanvas`, `Lite/Full 모델 스위치`, `WebGPU` 순으로 검토한다.

## 7. 핵심 기능 정의

### 기능 A. 세션 시작/권한 UX
- 시작 버튼을 누르기 전에는 카메라를 요청하지 않는다.
- 카메라가 필요한 이유와 로컬 처리 원칙을 짧게 설명한다.
- 오디오 역시 시작 버튼 이후에만 활성화한다.

### 기능 B. 손 추론 파이프라인
- 비디오 스트림 수신
- 워커에 프레임 전달
- Gesture Recognizer 추론
- 랜드마크/gesture/postprocess 결과를 메인 스레드에 전달
- 메인 스레드는 렌더링과 사운드 매핑만 담당

### 기능 C. 인터랙션 매핑
- `Open Palm`: 입자 방출 활성화, 기본 드론 사운드 온
- `Pinch`: 입자 밀도 증가, 색온도 변화, 필터 cutoff 상승
- `Victory`: 씬 모드 전환 또는 이펙트 토글

### 기능 D. 종료/복구
- 세션 종료 시 `MediaStreamTrack.stop()` 처리
- 인식기와 오디오 리소스를 명시적으로 해제
- 권한 거부, 카메라 없음, 성능 저하 상황에 대한 안내 메시지 제공

## 8. 시스템 구조 초안

```text
UI Shell
  -> Session Controller
    -> Camera Manager
    -> Inference Worker
      -> Gesture Recognizer
      -> Landmark Postprocess
    -> Interaction Mapper
      -> Visual Engine (three.js)
      -> Audio Engine (Tone.js)
```

### 권장 디렉토리 구조

```text
src/
  app/
  components/
  features/
    session/
    camera/
    inference/
    gestures/
    visual/
    audio/
  workers/
  shared/
```

### 상태 모델
- `idle`: 랜딩 전
- `priming`: 권한 요청 및 리소스 준비
- `running`: 체험 진행 중
- `paused`: 일시 정지 또는 포커스 이탈
- `error`: 권한 거부, 장치 미지원, 초기화 실패

## 9. 마일스톤

| 단계 | 목표 | 산출물 |
|---|---|---|
| Phase 0 | 프로젝트 부트스트랩 | Vite 앱, 기본 레이아웃, 상태 모델, 환경 설정 |
| Phase 1 | 입력 검증 | 카메라 시작/중지, 권한 UX, 비디오 프리뷰 |
| Phase 2 | 손 인식 검증 | 워커 기반 추론, 랜드마크/gesture 디버그 오버레이 |
| Phase 3 | 미디어아트 MVP | 파티클 씬 1종, 오디오 매핑 1종, 기본 제스처 3종 |
| Phase 4 | 제품 다듬기 | 에러 처리, 성능 튜닝, Safari 1차 대응 |
| Phase 5 | 확장 판단 | 추가 씬, 커스텀 제스처, 고성능 최적화 여부 결정 |

## 10. 우선순위 백로그

### P0
- Vite + TypeScript + React 프로젝트 초기화
- 전체 화면 캔버스 레이아웃과 랜딩 화면 작성
- 카메라 권한 안내 및 시작 버튼 구현
- Gesture Recognizer 워커 기반 실행 구조 검증
- 랜드마크 디버그 오버레이 구현

### P1
- `Open Palm`, `Pinch`, `Victory` 이벤트 정의
- three.js 파티클 씬 1종 구현
- Tone.js 시작/정지 및 파라미터 매핑 구현
- 세션 종료와 리소스 해제 처리

### P2
- 성능 패널 및 품질 preset 추가
- iOS Safari 예외 처리
- 안내 오버레이와 제스처 힌트 개선

## 11. 리스크와 대응

| 리스크 | 영향 | 대응 |
|---|---|---|
| Safari에서 성능 또는 호환성 저하 | 모바일 체험 품질 저하 | Chrome 우선 개발 후 Safari 대응 분리 |
| Gesture canned class 안정성 부족 | 오작동 증가 | 랜드마크 기반 규칙 제스처로 축소 |
| 오디오 자동재생 정책 이슈 | 소리 재생 실패 | 최초 사용자 액션에서만 Tone 시작 |
| 추론과 렌더링 경합 | 프레임 드랍 | 워커 분리, 추론 fps 제한, 디버그 모드 분리 |
| 조명/배경에 따른 인식 품질 편차 | 전시 현장 불안정 | 시작 전 안내 문구, 조도 테스트, 제스처 수 축소 |

## 12. 선결정이 필요한 항목

현재는 아래 항목을 다음과 같이 가정하고 시작한다.

- 타깃 환경: 데스크톱 Chrome 중심
- 배포 형태: 정적 호스팅 웹앱
- 작품 형태: 단일 페이지 몰입형 인터랙션
- 입력 장치: 웹캠 1대
- 출력 장치: 일반 스피커 또는 헤드폰

추가로 확정이 필요하지만 개발 시작을 막을 정도는 아닌 항목은 아래와 같다.

- 전시형 작품인지, 포트폴리오 데모인지
- 세로형 모바일 화면을 주 타깃으로 볼지 여부
- 사운드가 필수인지 선택 요소인지
- 최종 비주얼 톤앤매너

## 13. 이번 주 실행 계획

1. 프로젝트 부트스트랩과 기본 화면 구조를 만든다.
2. 카메라 시작/정지와 권한 UX를 완성한다.
3. Gesture Recognizer를 워커에서 돌리고 디버그 오버레이를 확인한다.
4. `Pinch` 연속값을 추출해 시각 효과 하나에 연결한다.
5. Tone.js를 붙여 시청각 동시 반응이 실제로 성립하는지 검증한다.

## 14. 다음 산출물 제안

이 기획서 다음으로 바로 만들 문서는 아래 순서가 적절하다.

1. `implementation-plan.md`: 세부 개발 태스크와 모듈별 작업 분해
2. `art-direction.md`: 비주얼 레퍼런스, 색/질감/모션 방향
3. `interaction-spec.md`: 제스처 정의, 임계값, 상태 머신
